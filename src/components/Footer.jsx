import React from 'react';
import '../style/footer.css';

const Footer = ({title}) => {
    return (
        <footer id="footer">
            <h2>{title} 정보 </h2>
        </footer>
    );
};

export default Footer;